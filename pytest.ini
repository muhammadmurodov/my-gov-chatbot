[pytest]
# Only collect the real suite. The notebooks/ dir has scripts (e.g. search_test.py)
# that load ~2.2 GB models at import time and OOM a busy GPU during collection —
# keep them out of `pytest` runs.
testpaths = tests
